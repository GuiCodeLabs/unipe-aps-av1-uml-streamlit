# Questão 09 — Coleção de CDs (Variação A)

## Prompt 01 — Análise
Atue como um Especialista Sênior em Engenharia de Prompt, Análise e Projeto de Sistemas. Apresente a análise estrita textual atualizando os pormenores para o cadastro de uma interface gerindo Coleções de CDs do tipo Variação A.

Contexto central a focar: Todo processo lida com a matriz de um elemento CD o qual possui identificadores condicionais (se é classificado flagrante de coletânea musical, se é caracterizado em material físico duplo de CDs extras acoplados). Este modelo possuirá no escopo iterativo musical uma vasta atrelação baseada em lista de múltiplas unidades contidas por título e respectiva temporalidade física (duração de faixa). Se a configuração for Coletânea ativada, a lista será atrelada a variados vetores em Músicos específicos associados na modelagem. Por fim as listagens de balanços responderão dinamicamente em painéis à base acadêmica em consultas exigindo atrelamentos interativos respondendo 1) Lista de todos lançamentos por músicos do filtro e 2) Em quais instâncias de coleções de material o título de uma trilha contida musical se encontra em escopo geral atrelado listado. O problema exige expressamente o mapeamento de **relacionamentos dispostos no formato de Atributos Derivados**.

Suas instruções textuais formativas explícitas perante entrega isolada em modelagem de projeto inicial:
1. Defina explicitamente sem abstração quais arquiteturas em agrupamentos isolados são as Classes deste ambiente com nomes precisos contidos e listagens tipando atrelado na restrição sobre Atributos Derivados como a exigência matricial. Exerça o refino da especificação.
2. Formate e separe a entrega de classes, destrinchando suas nomenclaturas atualizando os métodos respondendo operativamente sobre os dados listados associados perante os dois relatórios filtrados das exibições. Tudo sem injetar notações diagramáticas visuais nesta fase textual.

## Prompt 02 — Requisitos funcionais
Atue como Analista Estruturador focado em Engenharia de Software no viés Requisitos. Estruture com numerações precisas formais o rol das submissões de funcionalidades atreladas aos CDs baseadas na análise gerida na documentação textual prévia perante arquiteturas de colecionismo físico.

Contextos funcionais restritivos e métricas associadas: A interface registrará instâncias parametrizadas de flag condicional para Coletânea duplo etc. Associará músicas internas nas coleções, contendo validações relativas a vetores em arranjos musicais em Músicos nas coletâneas e exibirá exibições associadas aos painéis relatando em filtros iterativos Músicos nos Discos e Trilhas em seus referidos compartimentos do objeto físico focado internamente de base.

Suas formatações processuais para o rol listado nesta resposta imperativa:
1. Produza numericamente padrão literal RF contendo verbos base formativos da rotina sistêmica do ecossistema de bibliotecas num ambiente focado e coeso isento da formatações adicionais de clientes/logins ou fretes estéticos comerciais limitando foco na Variação A acadêmica matriz e os relatórios propostos na mesma base em escopo da questão base iterativa limitante e objetiva no painel base do relatório.

## Prompt 03 — Requisitos não funcionais
Atue formativamente exercendo atribuições da área técnica perante elaboração focada em documentador especialista mapeador no viés dos requisitos em categorias (RNF).

A moldura limitante: Arquitetura rodada numa UI renderizadora sob a via exclusiva ao `streamlit` escrito nos códigos estritos Python rodando internamente com as listas das variações e das músicas salvas numa via virtual isoladamente iterativa restrita dentro limpo via manipulável estrutura em estado (state) isolando conexões fora nativamente.

Sua diretiva perante aos arquivos criados antes no processo em resposta:
1. Estabeleça categóricas restrições enumeradas referendadas padronizadamente via RNF de limitações visuais das tecnologias sem margens. Mantenha tom voltado à avaliação rigorosa base da Variação do pacote focada na aplicação limpa e de fácil clonagem no ecossistema sem integrações robustas para rodar isoladamente e validar funções do motor acadêmico em testes manuais na disciplina das variações de engenharia focadas pela universidade restrita em seu painel visual iterativo no Streamlit contido de dados simulados geridos.

## Prompt 04 — Diagrama de classes
Atue rigidamente na restrição isoladamente de resposta arquitetural gráfica operando notações analítica orientadas via UML modelado focado como Arquiteto restrito plant formatando modelo de Classes base ao repositório limitativo desta versão da matriz focada à Variação A nas coleções sobre a classe de CDs criados na prova baseadas em restrições únicas para a arquitetura desta prova final. 

**ALERTA DE RESTRIÇÃO ABSOLUTA DA QUESTÃO**: Em contraste a outras, **neste diagrama é expressamente PROIBIDO exibir nomes dos parâmetros nos blocos contendo os atributos ou detalhar nomes de métodos.** A resolução obriga explicitamente fornecer um diagrama de classes "SOMENTE COM O NOME DAS CLASSES", mas "Especificando rigorosamente as representações físicas nos relacionamentos em linhas pontilhadas ou fixas com seus vetores das multiplicidades" no diagrama vazio focado nestas pontas em interações num formato macro do PlantUML da biblioteca.

Instruções base de formatação na linguagem final retornada:
1. Devolva apenas o código final em bloco tag `@startuml` a `@enduml`. 
2. Insira nomes limpos das caixas sem expô-las com atributos ou tipo de variáveis visando atender 100% da restrição em que caixas expostas terão unicamente isolado a parte do pacote relativo título da Classe, associando conectores explícitos exibindo em texto a multiplicidade exata (`1..*`, `1..1`, `0..*`) nas amarras das relações entre a entidade CDs para com suas entidades de Músicos e Trilhas Músicas contidos do modelo sem adentrar comportamentais do projeto base na tela iterativamente do visualizador de arquivos do PlantUML raw code entregável, sob pena formativa em erro crítico nas saídas indesejadas listáveis além desta formatação única.

## Prompt 05 — Aplicação Streamlit
Atue como Desenvolvedor focado nativamente na rotina backend UI em Python utilizando manipulação estática interativa ágil via Streamlit estruturando na sintaxe coesa focada visualmente a aplicação contida sobre "Coleções Variação A" e interações iterativas da universidade sob a biblioteca em painéis de formulários.

O contexto em tela gerador: Criações modulares nos blocos submetendo se instâncias iterativas e boolenas iterativas relativas sobre tipos contidos do álbum (duplos). Os painéis listarão faixas associativas isoladas (Tempo e Título), bem como manipulações permitindo cadastro atrelamentos temporais nativamente isolados da lógica se for "coletânea". Para atender a demanda central o layout deverá contar nos menus o visual exibindo cruzamentos filtrados (qual musico x tocou e mostrar, qual faixa y em discos listadas exibir tabela pandas analítica base iterativa listando dados da collection filtrada num visual dashboard ativado) geridos apenas pelas lists em estado ativo iterativamente formatadas da classe iterada.

Suas obrigações frente à saída processada:
1. Monte centralizadamente o `app.py`. A restrição é armazenar a via transacional iterativamente simulando nos registros internos iteradores virtuais sem criar banco (adote classes iterativas gerenciadas sobre dicionários globais estritos limpos do cache volátil via chamada `session_state` limpa para agilizar exibições fluídas nas rotinas).
2. Valide lógicas sistêmicas isoladamente sem permitir falhas de execuções nas tabelas no layout (use menus via abas na lateral via barra `sidebar` focando isoladamente na área 1 do Cadastro para focar no cadastro limpo, do layout 2 nos Relatórios Dinâmicos pautadas no fechamento cruzado sem misturá-las visualmente do painel base, empregando tabelas estáticas no preenchimento final das variáveis contidas visualizadas interativas nas queries do dashboard isolado do Python puro mantendo as sub-tabelas responsivas e coerentes do app limpo e de código coeso universitário final modular sob padrão avançado na sintaxe do motor na submissão em métodos em app.py único.

## Prompt 06 — requirements.txt
Atue sob um manto formativo devOps voltado restritivamente as empacotamentos básicos sem viés formativo explicativo focado apenas no mapeamento num texto num formato puro manifesto à base instalável base rodando no laboratório.

Diretriz processual do output gerador restrito da entrega a colar o material isolado ao executável do app na biblioteca das interações com dashboards iterativos nas relações cruzadas e agrupamentos das musicas formatados nos dataframes limpos baseados sob tabelagens geridas formatadas na exibição de dados e do frame stream nativo:
1. Construa e entregue exclusivamente as formatações atreladas diretas na base raw contidas para este pacote `requirements.txt`. Nem uma linha discursiva sua, fora as estritas bibliotecas (`streamlit`, tabulações etc) se justificar nativamente em text limitativo puramente, finalizando seu preceitual nesta saída.

## Prompt 07 — README da questão
Atue preenchendo as normativas estruturais como líder e escritor técnico da área acadêmica detalhando no formato `.md` um sumário de alto padrão relatando o ciclo de software focando o case explícito do problema "Coleção Variação A" sob as devidas arquiteturas geradas nos requisitos e no layout iterativo sem dados no database focados nas tabelagens na sessão volátil.

Ordens do formato na interface descritiva em respostas textuais contidas do markdown a submeter:
1. Descreva formativamente e profissional o pacote acadêmico submetido à disciplina sob layout organizado atreladamente às sub pastas exigências iterando referencial perfeitamente visualmente.
2. Defina o porquê escolhas limpas restritas formatam o modelar associativo isolado neste referencial sem banco físico externalizado e qual a usabilidade nos relatórios de fatiamentos de musicos x discos implementado e as estritas limitações aplicativamente nas notações UML limpas do Plant contidos no pacote diagramação simplificado formatado da listagem na variação do cenário A aplicativo submetido isoladamente formatados sob documentação visual rica (headings/blocos de código na chamada das execuções do run streamlit local) focado nas disciplinas para rápida assimilação aos professores nas entregas finais limpas da avaliação matriz sem exageros corporativos fantasiosos alheios.

## Prompt 08 — Refino final
Atue preenchendo seu lado QA. Como um Arquiteto Auditor na pipeline de conformidade, processe em análise final revisiva iterativa dos subprodutos (Análise/Req/App e Markdown gerado restritamente aplicativa na solução focada no laboratório) atestando que os atrelamentos isolados restritamente sem erros em relatórios nas análises dos relacionamentos.

Checklists imperativos formativos da área técnica nas orientações avaliativas entregáveis na devolutiva:
1. A base avaliativa é restritamente voltada a focar do porquê o Diagramador no processo da prova precisou estar completamente livre (em branco os miolos) constando restritivamente relacionamentos contendo atrelações puramente de nomenclatura nas bordas atestando as ordens e exigindo refração pesada, no modelo estrito listado de avaliações, onde as variáveis do diagrama, caso inseridas quebre a métrica matricial estabelecido limitante deste modelo de Diagrama só nome avaliado no lab focadamente com rigor no texto de análise entregável, apontando isso sob relatórios da avaliação textuais se aprovado na interface a checagem rigorosamente na submissão final dos arquivos em pastas formativamente iterativas em código e arquiteturalmente consistentes isoladamente da sintaxe da base da UML formativa isoladamente estritamente focada perante a modelagem avaliativa finalística desta variação A aplicada e listada ao projeto limpo.
